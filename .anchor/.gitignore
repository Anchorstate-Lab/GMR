# 判据声明跟着 git；日志和制品不跟。
#
# 日志只增不改：每次 observe 都追加条目，那正是设计要的，也正是一个被提交的
# 文件不该有的样子。`sync` 能从声明重建它，但重建捕获的是**此刻**的基准，
# 不是当初那次 —— 日志里的历史重建不回来。
#
# 制品是本机编译出来的，带着 platform 和二进制哈希，跨平台不通用。
*
!.gitignore
!anchors.toml
!probes.toml
