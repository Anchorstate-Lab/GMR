GMR 是一个领域无关的**接地记忆运行时**：让主观记录挂靠在客观可重算的观测上，在世界**沿作者声明的方向**变化时把它重新交出来。

**这是 infra，不是全能产品。** 工具给接口不给清单，语义归使用者。

架构 SSOT = [`GMR.md`](GMR.md)。机械可查的约束在 [`gate.sh`](gate.sh) 与测试里。**本文档只写那两处都抓不到的**：决定、纪律、红牌、死概念。

---

# 一、这十三条是 owner 定的，不要重新论证

```
1   锚定层是一台状态机。转换函数 δ(state, obs, 时间) → state'
2   探针位置住在 state 里，由域设置 —— 不是插件的输出
3   位置是域定义的类型。基底只认识「有位置这个槽」，不认识槽里写的是什么
4   没有固定状态词表。状态由域定义，基底不解释
5   插件 = 内容寻址的可执行物。版本 = 哈希，挣来的
6   插件吐状态向量（表示）；锚上选方向 + 写转换条件（注意力）
7   转换条件的输入只有三样：state · obs · 时间量。基底不提供别的派生量
8   终结态在锚上声明，基底机械兑现不可逆
9   记忆绑在锚上。绑定说「关于什么」，订阅说「什么时候」
10  转换条件写成规则表（守卫 → 新状态），第一条匹配即生效
11  state 是可寻址的结构，不是黑盒 —— 基底取得出字段，不解释字段含义
12  物理三层：基底（不产二进制） · 电池（可复用实现） · 域（插件+锚+装配+CLI）
13  语言必须有对象构造 —— 转换条件要吐出完整的新状态
```

**决定和论证分开存放，这一节只存决定。** 论证读起来越漂亮越危险 —— 抄录一段论证，等于把它重新装上膛。

---

# 二、三条硬纪律

**A. 「我看不见它」不等于「它不存在」。** 断言某个用例是纸面的、某个需求没有真实场景、某个能力没人用之前 —— **问**。这个仓库不是世界的全集。

**B. 删除真实实现之前必须问，无论论证多漂亮。** 论证的说服力和它的正确性无关。

**C. 不要给自己的决定写辩护。** 写下「做了什么」和「是谁定的」，不要写「为什么这样才对」。

> 三条都来自同一次事故：项目第一天有两个域，九天后 `domains/` 没了，trading 的实现和测试被删光。起点是一次事实误判 —— trading 的真实实现在另一个仓库，AI 看不见就当它是纸面的；随后用「只剩一个实现」论证「本来就不该拆分」。**先毁掉证据，再引用证据的缺席。**

---

# 三、停下来问 owner

当你即将 ①声称基底「做不到」某事、②决定该锚什么 / 盯哪个方向、③划一条职责或能力的边界、④判定某件事**不该被记录** —— **停**。这几乎都是语义判断。不确定就问，**别自己造一条边界当事实说出口**。

**④ 单列，因为它是唯一一个「什么都不做」就能犯的。** 遇到新的失败路径，「这不是关于世界的事实，所以不记」字面上对，落地就是静默通道。正确的问法是：它是不是「关于我们的失败」？**系统里不允许存在既不成条目、也不发边沿的路径。**

**硬绊线：** 锚报出转换时，改代码还是**改判据**（换插件 / 改转换表 / 挪终结态）？改判据 = 断言「我接受这次变化」或「我当初盯错了方向」= owner 的事。它看着像例行工具操作，这正是危险所在。

> 基底强制每次修订携带密封的理由，但只担保它**不可篡改**，不担保它**成立**。橡皮图章式的修订在数据上和真判断长得一模一样。

---

# 四、红牌 —— 违反了不会有人发现的那些

- **基底接受不可重算的断言。** 任何「谁说的」式的信任分级不得回归。
- **把插件作者或部署方的美德，说成基底的保证。** 已经犯过四次，每次都是**说得比能担保的更硬**。GMR 自己 ship 的传输与探针不适用「那是作者的美德」这句免责。
- **一跳派生规则没有记下自己的版本号。** 缺了它，日志里的值分不清是当时算错了还是后来规则换了 —— 两者处置完全相反。被 exec 的脚本要自己报哈希，声明的哈希盖不住它。
- **基底认识值的类型，或开始替作者做判断。** 分档靠多开锚，阈值归读模型。
- **基底定死一套状态词表。** 一旦给出，第二个域只能伪装成第一个的形状。
- **把装配写死在基底里。** 选哪个传输 / 提供方 / 后端是域的决定。**最容易被漏掉，因为它看起来像默认配置。**
- **ship 一份「该检测什么」的清单。** 主语是基底和产品都不行。**没真做模板，就不要在任何地方声称有。**
- **签名撒谎。** 可变的东西不许伪装成只增不改。
- **没有实现路径的 API。**
- **注释或文档替机制干活。** 一段声明「我不是工具的一部分」的注释，说明该动的是结构。

---

# 五、死概念 —— 不要重新引入

- **逐方向的参照向量** `Baseline` · `BaselineEntry` · `FacetValue`
- **把「挪参照」「换位置」「换派生规则」分成三种修订** —— 它们都是一次状态转换
- **基底每次替域算一个差集并存进条目** `Delta` · `delta_of`
- **固定的期望四态 / 三段式期望** `Expect` · `ExpectValue`
- **逐方向的三态结果表** `FacetOutcome` · `FacetName` · `Facets`
- **基底提供跨观测的派生量** `streak` · `prev` · `located_at`
- **域作为一个编译进来的、需要 schema 与声明式版本号的组件** —— 但这**不等于**「域不该物理分离」
- **来源的信任分级 / 给断言打分的断言**
- **事实存储 / 中间物归档** `FactStore` · `SourceArchive`
- **裁决器版本 / 基底封的裁决 / 基底定义的状态词表**
- **记忆内容进基底** —— 基底存引用与版本，靠提供方按版本取回，不复制
- **正则扫源码的架构规则清单** —— 它锚的是**契约的影子**，不是契约
- **被 ship 的预设检测清单** —— 机制活着，清单死着

> **反引号里的名字是检测面，不是给人读的。** 文档约束禁止写类型名，这里是它自己写明的例外。去掉它们，`dead::*` 那批锚就失去了人可读的出处 —— 已经发生过一次。

---

# 六、文档自我约束

只写长期契约与立场。**禁止**写入：具体类型名 / 字段名 / 函数名 / 常量数值 / 文件名 / 调用链 —— 例外只有上面那份死概念清单。

**机械可查的约束写成可执行的检查，不写进这里。** 不写验收清单：自己列检查表、用它给自己打分，是这个项目已经犯过的错误。

**不写走查例子。** 例子会被下一个人读成清单，而下一个真正有人在乎的方向不会在任何清单上。
