# 自成一个 workspace。**探针是 exec 的不是链接的** —— 每种探针单独管理，
# 只在报告形状上跟别的探针一致（gmr-probe-coord）。
[workspace]

[package]
name = "name-map"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1"
gmr-probe-coord = { path = "../probe-coord" }
