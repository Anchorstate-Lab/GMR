# 探针是 exec 的不是链接的 —— 每种探针单独管理，只在报告形状上跟别的
# 探针一致（gmr-probe-coord）。member，跟 addr-map/ast-map/name-map/
# prose-map/coord 共用 batteries/probes/ 这一个 workspace。

[package]
name = "addr-map"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde_json = { workspace = true }
gmr-probe-coord = { workspace = true }

[lints]
workspace = true
