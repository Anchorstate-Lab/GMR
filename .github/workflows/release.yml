name: Release

on:
  push:
    tags: [ "v*" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # One binary. The extractors are linked into it, so nothing else travels —
  # and this repository's own test-roster probe is bootstrap data, not product.
  tarball:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - { os: macos-latest,  target: aarch64-apple-darwin }
          - { os: macos-13,      target: x86_64-apple-darwin }
          - { os: ubuntu-latest, target: x86_64-unknown-linux-gnu }
          - { os: ubuntu-24.04-arm, target: aarch64-unknown-linux-gnu }
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: cargo build --release -p coding-anchor

      - name: Stage
        run: |
          mkdir -p dist/out/bin
          cp target/release/gmr dist/out/bin/gmr

      - name: Prove it works before shipping it
        run: sh acceptance.sh

      - name: Pack
        run: tar -czf gmr-${{ matrix.target }}.tar.gz -C dist/out bin

      - uses: actions/upload-artifact@v4
        with:
          name: gmr-${{ matrix.target }}
          path: gmr-${{ matrix.target }}.tar.gz

  publish:
    needs: tarball
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with: { path: assets, merge-multiple: true }

      - name: Attach the tarballs to the release
        uses: softprops/action-gh-release@v2
        with:
          files: assets/*.tar.gz
          fail_on_unmatched_files: true
